

/**
*
*Esta funcion devuelve una promesa, segun las especificaciones de acuerdo a los parametros
*que ingrese el desarrolador
*@param { String } route
*@param { object } options
* @returns promise
*
*/
export const mdLinks = (route, options) => {
   return new Promise((resolve, reject) => {
       resolve([])
   })
};