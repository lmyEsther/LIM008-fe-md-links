// en este archivo deberia ir funciones que cuente la cantidad total de links y calcule los links unicos


/**
 * Esta funcion deberia contar la cantidad total de links que se encuentren almacenados en el array
 * @param {array de links almacenados luego de haber leido el md} arrayDeLinks 
 * 
 * @returns number
 * 
 */

const cantidadTotalDeLinks = (arrayDeLinks) => {
    return array.length
}

/**
 * 
 * @param {Array} arrayDeLinks 
 * 
 * @returns {Array} array de links unicos
 * 
 * 
 */
const filtrarLinksUnicos = (arrayDeLinks) => {

}

/**
 * 
 * @param {Array} arrayDeLinks 
 * @returns {number} total de links unicos
 * 
 */
const contarLinksUnicos = (arrayDeLinks) => {

    return array.length
}
